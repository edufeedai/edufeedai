# Configuración de EduFeedAI

# API Key de OpenAI (requerida para process, check, download)
OPENAI_API_KEY=sk-your-api-key-here

# Directorio de trabajo (opcional, por defecto usa el directorio actual)
# Si se especifica, todos los archivos (ZIP de entregas, base de datos, etc.)
# se procesarán en este directorio
# WORK_DIR=/ruta/a/tu/directorio/de/trabajo
